namespace sistema.Classes
{
    public class Strings
    {

      public string FILTRO()
        {   
            var SEND  = "DETALHES E FILTRO DE DADOS: ";
            return SEND;
        }

        public string SAVE()
        {   
            var SEND  = "SALVANDO DADOS: ";
            return SEND;
        }

        public string UPDATE()
        {   
            var SEND  = "ATUALIZANDO DADOS: ";
            return SEND;
        }  
        public string DELETE()
        {   
            var SEND  = "DELETANDO DADOS: ";
            return SEND;
        }  

        public string PERMISSION()
        {   
            var SEND  = "ACESSANDO PERMISSÕES: ";
            return SEND;
        }                      
    }
}
